{% extends 'base.html.twig' %}
{% block title %}List places{% endblock %}
{% block main %}
    {{ parent() }}

        <!-- Projects-->
        <section class="projects-section bg-light" id="projects">
            <div class="container px-4 px-lg-5">

                <!-- Project Two Row-->
                <div class="row gx-0 justify-content-center">
                    <div class="col-lg-12 order-lg-first">
                        <div style="margin-bottom: 30px;" >
                            <a href="{{ path('app_place_new') }}">+Create new</a>
                            <a  style="margin-left: 60%;" href="{{ path('place_search')}}" >Search</a>
                        </div>
                        <h1>List place</h1>


                        {# LISTADO DE PELÍCULAS #}
                        {% include '/place/only_list.html.twig' %}

                        {# PAGINADOR #}
                        {% 
                            include '/includes/paginator.html.twig' with
                            {
                                'route': 'app_place_index',
                                'entidades': 'place',
                            }
                         %}


                    </div>
                </div>
            </div>
        </section>



{% endblock %}








